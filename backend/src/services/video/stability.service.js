import axios from 'axios'
const STABILITY_KEY = process.env.STABILITY_API_KEY || ''
export default {
  async generate(scenePrompt, opts = {}) {
    // Example using Stability API — implement per their docs
    throw new Error('Stability video integration not implemented yet')
  }
}
